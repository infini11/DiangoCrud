nb : ne creer pas l'environnement virtuel a l'interieur du projet. Met le cote a cote avec le projet et le tout tu le mets sur un dossier 

taches:
- creer une application django en utlisant la commande : python manage.py #nomApp(le nom que tu voudras) polls
- configurer la base de donnees avec mysql
- se deplacer dans djangocrude(projet) et faire la commande pip freeze > requirements.txt(pour renseigner les dependances)
- pour la classe tache : creer un model et faire les migrations sur la base de donnees
	les champ : nomtache, description, debut et fin
- creer un super utilisateur pour avoir acces au niveau de l'interface admin
- creer une page d'accueil avec un lien hypertext pour acceder a la formulaire
- creer un formulaire pour contenant les champ d'une tache

